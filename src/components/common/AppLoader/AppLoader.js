import React from 'react';
import LinearProgress from '@material-ui/core/LinearProgress';

const AppLoader = () => {
  return <LinearProgress color="secondary" className="app-loader" />;
};

export default AppLoader;
